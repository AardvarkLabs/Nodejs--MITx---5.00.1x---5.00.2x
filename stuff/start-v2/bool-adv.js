
// if advaced mode 



let isaccLock = true
let userRole = 'user'


// new lines of if 

if (false) {
      console.log('locked up')
} else if (userRole === 'admin') { 
    console.log('welcome admin')
} else {
  console.log('welcome')
}


  // challenge area

  let temp = 45 

  if (temp = 32) {

    if (temp <= 32) {
        console.log('it is freezing outside')
    } else if (temp >= 110) {
        console.log('it is hot outside')
    }
  }

  
  // it freezing outsite

  // it is hot outside 

  // got for it. it is pretty niceapp.listen(
      
